/**
 * lib/whatsapp.ts — WhatsApp numbers are collected for future use.
 * Evolution API has been removed. Numbers are stored in the DB only.
 */

export async function sendWelcomeWhatsApp(_phone: string, _email: string): Promise<void> {
  // WhatsApp messaging removed — number stored in DB for future use
}

export async function sendUpgradeWhatsApp(_phone: string, _plan: string): Promise<void> {
  // WhatsApp messaging removed — number stored in DB for future use
}
